Implement:
    Entity selection in elements state
        (select node, boundary point, element with left mouse button)
        Also in results state: display result value

    Vector field view modes selector (Vibrate, displacmeent
        Fit vs. absolute scale.

    Boundary pressure view mode
    Result Deletion
        Rewrite clear() so empty result nodes removed from the display

    Result browsing hotkeys (advance to next/prev result of the same type)
    Result/Settings Major Tree + flattened version
    MSH writer (multiple MSH needed for model changes).

    Fix bug when simultaneously changing models and settings

    ***Zoom/Pan*** in viewer (but keep aspect ratio... tricky).

    Debug cut cell stress computation... why is it so high?
